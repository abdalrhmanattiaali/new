# 🧪 دليل الاختبار الشامل للمساعد العائلي

## 📋 نظرة عامة

هذا الدليل يشرح كيفية اختبار جميع ميزات المساعد العائلي الذكي على عائلة مسجلة تجريبية، **بدون الحاجة إلى واتساب**.

## 🎯 ماذا يفعل الاختبار الشامل؟

السكريبت `test-full-features.js` يقوم بـ:

### 1️⃣ **إنشاء قاعدة بيانات تجريبية**
- قاعدة بيانات منفصلة في `data/test_family.db`
- لا يؤثر على قاعدة البيانات الحقيقية

### 2️⃣ **إنشاء عائلة تجريبية كاملة**
- **العائلة**: عائلة أحمد
- **الأب**: أحمد (+201234567890)
- **الأم**: فاطمة (+201234567891)
- **الطفل**: محمد (عمره 4 شهور)
- **هدف عائلي**: تطوير مهارات الجلوس

### 3️⃣ **اختبار جميع الخدمات**
1. ✅ **الرسائل اليومية المتنوعة** (Daily Messages)
2. ✅ **رسائل تطوير الطفل** (Child Development)
3. ✅ **تذكيرات المعالم الشهرية** (Monthly Milestone)
4. ✅ **خدمة الطقس اليومية** (Weather Service)
5. ✅ **تذكيرات المناسبات** (Anniversary Reminders)
6. ✅ **الرسائل التحفيزية** (Motivational Messages)
7. ✅ **خطة نهاية الأسبوع** (Weekend Planner)
8. ✅ **التقرير الأسبوعي** (Weekly Report)
9. ✅ **خدمة الأهداف** (Goals Service)

### 4️⃣ **طباعة جميع الرسائل المولدة**
- يطبع كل رسالة مولدة من GPT-5
- يعرض لمن ستُرسل الرسالة
- يعرض محتوى الرسالة كاملاً

---

## 🚀 كيفية الاستخدام

### الخطوة 1️⃣: إعداد API Key

افتح ملف `src/test-full-features.js` وعدّل السطر 10:

```javascript
global.OPENAI_API_KEY = 'sk-YOUR-REAL-OPENAI-KEY-HERE';
```

احصل على API Key من: https://platform.openai.com/api-keys

### الخطوة 2️⃣: تشغيل الاختبار

```bash
npm run test:full
```

### الخطوة 3️⃣: مراجعة النتائج

سترى:
1. **إنشاء العائلة التجريبية**
2. **اختبار كل خدمة** مع عرض الرسائل المولدة
3. **ملخص النتائج** في النهاية

---

## 📊 مثال على الناتج

```bash
======================================================================
🧪 اختبار شامل لجميع ميزات المساعد العائلي الذكي
======================================================================

📝 تحميل الإعدادات
----------------------------------------------------------------------
✅ تم تحميل الإعدادات بنجاح

📝 إعداد قاعدة البيانات التجريبية
----------------------------------------------------------------------
ℹ️  تم حذف قاعدة البيانات التجريبية القديمة
✅ تم إنشاء قاعدة البيانات التجريبية

📝 إنشاء عائلة تجريبية
----------------------------------------------------------------------
✅ تم إنشاء العائلة برقم: 1
✅ تم إضافة الأب: أحمد
✅ تم إضافة الأم: فاطمة
✅ تم إضافة الطفل: محمد (عمره 4 شهور - تاريخ الميلاد: 2025-07-09)

======================================================================
ℹ️  بدء اختبار جميع الخدمات...
======================================================================

📝 اختبار الرسائل اليومية المتنوعة
----------------------------------------------------------------------

رسالة إلى +201234567890:
----------------------------------------------------------------------
صباح الخير أحمد! 🌅

اليوم يوم جديد مليء بالفرص...
----------------------------------------------------------------------

✅ تم اختبار الرسائل اليومية المتنوعة بنجاح

📝 اختبار رسائل تطوير الطفل (4 شهور)
----------------------------------------------------------------------

رسالة تطوير الطفل إلى +201234567890:
----------------------------------------------------------------------
مرحباً أحمد! 👶

محمد الآن في شهره الرابع، وقت رائع للنمو...
----------------------------------------------------------------------

✅ تم اختبار رسائل تطوير الطفل بنجاح

... (باقي الخدمات)

======================================================================
📊 نتائج الاختبار الشامل
======================================================================
الإحصائيات:
✅ اختبارات نجحت: 9
❌ اختبارات فشلت: 0
📊 إجمالي الاختبارات: 9
⏱️  المدة: 45.23 ثانية

 🎉 جميع الاختبارات نجحت!

📝 معلومات قاعدة البيانات التجريبية
----------------------------------------------------------------------
ℹ️  مسار قاعدة البيانات: /path/to/data/test_family.db
ℹ️  يمكنك فتح هذا الملف باستخدام SQLite Browser لمراجعة البيانات

======================================================================
✨ انتهى الاختبار الشامل ✨
======================================================================
```

---

## 🔍 فحص البيانات التجريبية

بعد تشغيل الاختبار، يمكنك فحص قاعدة البيانات التجريبية:

### باستخدام SQLite Browser
1. قم بتحميل [DB Browser for SQLite](https://sqlitebrowser.org/)
2. افتح ملف `data/test_family.db`
3. استكشف الجداول:
   - `families` - العائلات
   - `guardians` - الأولياء
   - `children` - الأطفال
   - `message_log` - سجل الرسائل
   - `family_goals` - أهداف العائلة

### باستخدام سطر الأوامر
```bash
sqlite3 data/test_family.db

# عرض جميع العائلات
SELECT * FROM families;

# عرض جميع الأطفال
SELECT * FROM children;

# عرض سجل الرسائل
SELECT * FROM message_log;
```

---

## 🎛️ تخصيص الاختبار

### تغيير بيانات الطفل

في ملف `src/test-full-features.js`، السطر 202-208:

```javascript
// إضافة طفل عمره 6 شهور بدلاً من 4
const childBirthDate = new Date();
childBirthDate.setMonth(childBirthDate.getMonth() - 6); // غيّر هنا
```

### إضافة المزيد من الأطفال

```javascript
// طفل ثاني
const child2BirthDate = new Date();
child2BirthDate.setFullYear(child2BirthDate.getFullYear() - 2); // عمره سنتين

db.prepare(`
  INSERT INTO children (family_id, name, birth_date, gender, allergies)
  VALUES (?, ?, ?, ?, ?)
`).run(familyId, 'سارة', child2BirthDate.toISOString().split('T')[0], 'female', null);
```

### إضافة المزيد من الأهداف

```javascript
db.prepare(`
  INSERT INTO family_goals (family_id, title, description, target_date, status)
  VALUES (?, ?, ?, ?, ?)
`).run(
  familyId,
  'تعليم الأبجدية',
  'نريد أن يتعلم الطفل الأبجدية العربية',
  targetDate.toISOString().split('T')[0],
  'active'
);
```

---

## 🐛 استكشاف الأخطاء

### مشكلة: "OPENAI_API_KEY not found"
**الحل**: تأكد من تعديل API Key في السطر 10 من `src/test-full-features.js`

### مشكلة: "Error: ENOENT"
**الحل**: تأكد من تشغيل الأمر من مجلد المشروع الرئيسي

### مشكلة: "Database is locked"
**الحل**:
1. أغلق أي برنامج يستخدم قاعدة البيانات
2. احذف ملف `data/test_family.db` وأعد التشغيل

### مشكلة: بعض الخدمات فشلت
**الحل**: تحقق من:
- اتصال الإنترنت (لخدمة الطقس)
- API Key صحيح
- الملفات المطلوبة موجودة

---

## 📝 ملاحظات مهمة

1. ✅ **آمن تماماً**: لا يؤثر على قاعدة البيانات الحقيقية
2. ✅ **بدون واتساب**: لا يحتاج إلى WhatsApp Bot
3. ✅ **سريع**: يختبر جميع الخدمات في دقيقة واحدة
4. ✅ **شامل**: يختبر 9 خدمات مختلفة
5. ⚠️ **يحتاج API Key**: يجب تعديل API Key قبل التشغيل

---

## 🔄 مقارنة مع الاختبارات الأخرى

| الاختبار | الوصف | الاستخدام |
|----------|-------|----------|
| `npm run test:ai` | اختبار الذكاء الاصطناعي فقط | `npm run test:ai` |
| `npm run test:full` | **اختبار شامل مع عائلة تجريبية** | `npm run test:full` |
| `npm start` | تشغيل البرنامج الكامل مع واتساب | `npm start` |

---

## 📚 ماذا بعد؟

بعد نجاح الاختبار الشامل:

1. ✅ تأكدت أن جميع الخدمات تعمل
2. ✅ شاهدت أمثلة حقيقية للرسائل المولدة
3. ✅ الآن جاهز لتشغيل البرنامج الحقيقي!

### للتشغيل الفعلي:
```bash
npm start
```

سيطلب منك مسح QR Code لربط واتساب، ثم يبدأ البرنامج في العمل مع العائلات الحقيقية!

---

## 🆘 الدعم

إذا واجهت أي مشاكل:
1. تحقق من قسم [استكشاف الأخطاء](#-استكشاف-الأخطاء)
2. راجع ملف `README.md` الرئيسي
3. تحقق من السجلات في `logs/app.log`

---

**صُنع بـ 💙 للعائلات العربية**

🎯 **اختبار شامل - تأكد من عمل كل شيء قبل الإطلاق!**
