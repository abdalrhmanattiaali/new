# 🎯 الميزات الجديدة | New Features

## v1.1 - إطلاق الميزات المحسّنة

---

## 1. 👨‍👩‍👧‍👦 إرسال رسائل للجروب العائلي

### الوصف
الآن يمكنك استقبال جميع الرسائل في جروب واحد يضم **الأب والأم معاً** بدلاً من إرسال رسائل فردية!

### المميزات
- ✅ إرسال موحد لجميع أفراد العائلة
- ✅ تقليل عدد الرسائل المكررة
- ✅ تفاعل جماعي مع التوصيات
- ✅ إمكانية الرجوع للإرسال الفردي

### كيفية التفعيل

#### الطريقة 1: من Onboarding (قريباً)
سيتم إضافة خطوة في عملية الإعداد الأولي لربط الجروب.

#### الطريقة 2: يدوياً من قاعدة البيانات

```bash
# 1. أنشئ جروب واتساب يضم الأب والأم
# 2. أضف البوت للجروب
# 3. احصل على معرف الجروب (Group ID)

# معرف الجروب يكون بالشكل التالي:
# 201234567890-1234567890@g.us

# 4. حدّث قاعدة البيانات:
sqlite3 data/family_assistant.db

UPDATE families
SET family_group_id = '201234567890-1234567890@g.us',
    send_to_group = 1
WHERE id = 1;

.exit
```

### الإعدادات في config.yaml

```yaml
groups:
  enabled: true
  send_to_group_by_default: true
  fallback_to_individual: true
```

---

## 2. 🌤️ تكامل حالة الطقس

### الوصف
الرسائل الآن **تأخذ حالة الطقس في الاعتبار** لاقتراح الأنشطة المناسبة!

### المميزات
- ☀️ بيانات طقس حية من OpenWeather API
- 🌡️ تقدير ذكي للطقس (إذا لم يتوفر API)
- 🎨 اقتراحات نشاطات حسب الجو
- ⚠️ تحذيرات من الحرارة الشديدة أو البرد

### أمثلة الاقتراحات

**جو حار (> 35°م)**
```
الجو حار جداً! يُفضل اللعب داخل المنزل والإكثار من السوائل. 🌡️
```

**جو ممطر**
```
الجو ممطر! وقت مثالي لألعاب منزلية أو قراءة قصص. ☔
```

**جو رائع (18-28°م)**
```
الجو رائع! وقت مثالي للعب في الخارج أو نزهة في الحديقة. 🌟
```

### الإعداد

1. احصل على مفتاح API مجاني من: https://openweathermap.org/api

2. أضفه في ملف `.env`:
```env
WEATHER_API_KEY=your_api_key_here
```

3. إذا لم تضف المفتاح، النظام سيستخدم **تقدير ذكي** حسب الموسم والمدينة

### الإعدادات

```yaml
weather:
  enabled: true
  provider: "openweather"  # أو "estimation"
  update_interval_minutes: 30
  include_in_messages: true
  show_for_activities:
    - child_play
    - weekend_outings
```

### اختبار الطقس

```bash
npm run test:weather
```

سيعرض:
```
📍 Cairo:
☀️ الطقس الآن في Cairo:
🌡️ الحرارة: 28°م
💨 الرياح: 15 كم/س
💧 الرطوبة: 45%

🌟 الجو رائع! وقت مثالي للعب في الخارج أو نزهة في الحديقة.
```

---

## 3. 🧪 لوحة اختبار شاملة (Test Panel)

### الوصف
**لوحة اختبار متكاملة** لتجربة جميع الخصائص بسهولة!

### الأوامر المتاحة

| الأمر | الوصف |
|-------|-------|
| `npm run test:weather` | اختبار خدمة الطقس |
| `npm run test:message child_play` | اختبار رسالة محددة |
| `npm run test:weekend` | اختبار خطة نهاية الأسبوع |
| `npm run test:report` | اختبار التقرير الأسبوعي |
| `npm run test:generate` | توليد رسائل اليوم |
| `npm run test:send` | إرسال الرسائل المجدولة |
| `npm run test:group` | اختبار الإرسال للجروب |
| `npm run test:all` | اختبار شامل لجميع الخصائص |

### أمثلة الاستخدام

```bash
# اختبار رسالة اللعب (مع الطقس)
npm run test:message child_play

# اختبار شامل
npm run test:all

# اختبار إعدادات الجروب
npm run test:group
```

### مخرجات مثال

```
🧪 لوحة اختبار مساعد واتساب العائلي

📋 الأوامر المتاحة:

1️⃣  test:weather          - اختبار حالة الطقس
2️⃣  test:message [type]   - اختبار رسالة محددة
3️⃣  test:weekend          - اختبار خطة نهاية الأسبوع
...
```

---

## 4. ⏰ إشعارات على مدار اليوم مع الطقس

### الوصف
الرسائل الآن **موزعة بذكاء على مدار اليوم** مع مراعاة:
- ⏰ التوقيت المحلي
- 🌤️ حالة الطقس
- 📍 المنطقة الزمنية
- 🔕 ساعات الهدوء

### جدول الرسائل اليومية

| الوقت | النوع | المحتوى |
|-------|------|---------|
| 07:30 صباحاً | روتين الصباح | نصائح الإفطار + النوم |
| 13:30 ظهراً | نشاط | لعب هادف + تطوير لغة |
| 19:00 مساءً | روتين المساء | تهدئة + قصة + دعم نفسي |

### التوزيع الذكي

النظام يأخذ في الاعتبار:

1. **الوقت المفضل** لكل والد
2. **حالة الطقس** للأنشطة الخارجية
3. **ساعات الهدوء** (22:00 - 06:30)
4. **عدم الإزعاج** بأكثر من رسالة في الساعة

### الإعدادات

```yaml
app:
  quiet_hours:
    start: "22:00"
    end: "06:30"
  rate_limits:
    max_msgs_per_day_per_family: 5
    min_gap_minutes: 60

tracks:
  density:
    morning: 1
    noon: 1
    evening: 1
```

---

## 📊 إحصائيات الميزات الجديدة

### الملفات المضافة
- ✅ `src/services/weatherService.js` (290 سطر)
- ✅ `src/utils/testPanel.js` (230 سطر)
- ✅ `src/test.js` (55 سطر)

### التحديثات
- ✅ `src/database/schema.js` - دعم الجروبات
- ✅ `src/services/messageEngine.js` - تكامل الطقس والجروبات
- ✅ `config/config.yaml` - إعدادات جديدة
- ✅ `package.json` - 9 أوامر اختبار

### إجمالي الكود الجديد
**~600 سطر** من الكود عالي الجودة!

---

## 🚀 كيفية التجربة

### 1. تحديث الإعدادات

```bash
# انسخ ملف .env الجديد
cp .env.example .env

# أضف مفتاح الطقس (اختياري)
# WEATHER_API_KEY=your_key_here
```

### 2. تحديث قاعدة البيانات

```bash
# حذف قاعدة البيانات القديمة
rm -f data/family_assistant.db

# إنشاء قاعدة بيانات جديدة
npm run db:init
```

### 3. تشغيل الاختبارات

```bash
# اختبار الطقس
npm run test:weather

# اختبار شامل
npm run test:all
```

### 4. التشغيل العادي

```bash
npm start
```

---

## 💡 نصائح الاستخدام

### للحصول على أفضل تجربة:

1. **استخدم الجروب العائلي**
   - أنشئ جروب يضم الأب والأم
   - أضف البوت للجروب
   - فعّل الإرسال للجروب

2. **فعّل الطقس**
   - احصل على مفتاح OpenWeather API مجاني
   - أضفه في `.env`
   - استمتع باقتراحات ذكية حسب الجو

3. **اختبر بانتظام**
   - استخدم `npm run test:all` للتأكد من عمل كل شيء
   - جرّب رسائل مختلفة بـ `npm run test:message [type]`

---

## 🆘 الدعم

إذا واجهت مشاكل:

1. راجع قسم [استكشاف الأخطاء](README.md#-استكشاف-الأخطاء) في README
2. شغّل `npm run test:all` لفحص النظام
3. افتح Issue على GitHub

---

**صُنع بـ 💙 للعائلات العربية**

جميع الميزات مدمجة بسلاسة وجاهزة للاستخدام الفوري!
