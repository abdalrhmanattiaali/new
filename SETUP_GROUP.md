# 👨‍👩‍👧‍👦 دليل إعداد الجروب العائلي
## Family Group Setup Guide

---

## 📋 نظرة عامة

هذا الدليل يشرح كيفية إعداد **الجروب العائلي** لاستقبال جميع الرسائل في مكان واحد يضم الأب والأم معاً.

---

## ✨ الفوائد

| قبل | بعد |
|-----|------|
| رسالة للأب + رسالة للأم = رسالتان | رسالة واحدة للجروب |
| تفاعل منفصل | تفاعل جماعي ومشاركة آراء |
| صعوبة المتابعة | سهولة الرجوع للرسائل السابقة |

---

## 🔧 خطوات الإعداد

### الخطوة 1: إنشاء الجروب

1. افتح واتساب
2. اضغط على **مجموعة جديدة** (New Group)
3. أضف الأب والأم للجروب
4. سمّي الجروب (مثلاً: "عائلة أحمد - المساعد الذكي")

✅ **الجروب جاهز!**

---

### الخطوة 2: إضافة البوت للجروب

1. افتح الجروب
2. اضغط على **اسم الجروب** أعلى الشاشة
3. اضغط على **إضافة مشاركين**
4. ابحث عن رقم البوت وأضفه

⚠️ **مهم:** تأكد أن البوت **مسجل** ومتصل بواتساب قبل إضافته

---

### الخطوة 3: الحصول على معرف الجروب (Group ID)

هناك طريقتان:

#### الطريقة الأولى: من واتساب ويب (الأسهل)

1. افتح https://web.whatsapp.com
2. امسح QR Code
3. افتح الجروب العائلي
4. انظر للرابط في المتصفح:
   ```
   https://web.whatsapp.com/accept?code=xxxxx
   ```
5. معرف الجروب سيكون بالشكل: `201234567890-1234567890@g.us`

#### الطريقة الثانية: من كود البوت

أضف هذا الكود المؤقت في `src/index.js`:

```javascript
// إضافة مؤقتة للحصول على معرف الجروب
bot.on('message', async (msg) => {
  if (msg.from.includes('@g.us')) {
    console.log('🆔 معرف الجروب:', msg.from);
  }
});
```

ثم أرسل أي رسالة في الجروب وانظر للسجلات (logs).

---

### الخطوة 4: حفظ معرف الجروب في قاعدة البيانات

```bash
# 1. افتح قاعدة البيانات
sqlite3 data/family_assistant.db

# 2. شاهد العائلات الموجودة
SELECT id, family_name FROM families;

# 3. حدّث معرف الجروب (استبدل القيم بالقيم الحقيقية)
UPDATE families
SET family_group_id = '201234567890-1234567890@g.us',
    send_to_group = 1
WHERE id = 1;

# 4. تأكد من التحديث
SELECT id, family_name, family_group_id, send_to_group FROM families;

# 5. اخرج
.exit
```

---

### الخطوة 5: تحديث ملف الجروبات المسموح بها

افتح `config/allowed_groups.json`، وابحث عن قسم `"groups"`. أضف معرف الجروب والاسم الوصفي كما في المثال (يمكنك ترك الاسم فارغًا إذا رغبت):

```json
{
  "groups": [
    {
      "id": "201234567890-1234567890@g.us",
      "label": "عائلة عبدالرحمن وشروق"
    }
  ]
}
```

> 🔐 لن يرسل البوت أي رسالة إلى جروب غير موجود في هذا الملف. إذا احتجت لمسار مختلف، استخدم المتغير البيئي `ALLOWED_GROUPS_PATH` وأعد تشغيل الخدمة.

---

### الخطوة 6: اختبار الإعداد

```bash
# اختبر إعدادات الجروب
npm run test:group
```

يجب أن ترى:

```
✅ الإعدادات صحيحة للإرسال للجروب
```

ثم جرّب إرسال رسالة اختبارية:

```bash
npm run test:message child_play
```

---

## 🎯 التأكد من نجاح الإعداد

### ✅ علامات النجاح:

1. البوت **موجود** في الجروب
2. معرف الجروب **محفوظ** في قاعدة البيانات
3. `send_to_group = 1` في قاعدة البيانات
4. اختبار `npm run test:group` **ناجح**
5. الرسائل تصل **للجروب** وليس للأفراد

### ❌ علامات الفشل:

| المشكلة | الحل |
|---------|------|
| البوت غير موجود في الجروب | أضفه من جديد |
| معرف الجروب خاطئ | تأكد من النسخ الصحيح |
| `send_to_group = 0` | حدّث قاعدة البيانات |
| الرسائل تذهب للأفراد | تحقق من الإعدادات |

---

## ⚙️ الإعدادات المتقدمة

### في ملف `config/config.yaml`:

```yaml
groups:
  enabled: true                    # تفعيل ميزة الجروبات
  send_to_group_by_default: true   # إرسال للجروب افتراضياً
  fallback_to_individual: true     # الرجوع للإرسال الفردي إذا فشل
```

### خيارات الإعداد:

| الخيار | القيمة | الوصف |
|--------|--------|-------|
| `enabled` | `true/false` | تفعيل/تعطيل الجروبات |
| `send_to_group_by_default` | `true/false` | الإرسال للجروب افتراضياً |
| `fallback_to_individual` | `true/false` | الرجوع للإرسال الفردي عند الفشل |

---

## 🔄 التبديل بين الجروب والإرسال الفردي

### الإرسال للجروب:

```sql
UPDATE families SET send_to_group = 1 WHERE id = 1;
```

### الإرسال الفردي:

```sql
UPDATE families SET send_to_group = 0 WHERE id = 1;
```

---

## 🐛 استكشاف الأخطاء

### المشكلة 1: "البوت لا يرسل للجروب"

**الأسباب المحتملة:**
1. معرف الجروب خاطئ
2. `send_to_group = 0`
3. البوت ليس في الجروب

**الحلول:**
```bash
# 1. تحقق من الإعدادات
sqlite3 data/family_assistant.db
SELECT family_group_id, send_to_group FROM families WHERE id = 1;

# 2. حدّث الإعدادات إذا لزم الأمر
UPDATE families SET send_to_group = 1 WHERE id = 1;

# 3. اختبر
npm run test:group
```

---

### المشكلة 2: "معرف الجروب غير صحيح"

**شكل معرف الجروب الصحيح:**
```
201234567890-1234567890@g.us
```

**شكل خاطئ:**
```
123456  ❌
group_name  ❌
+201234567890  ❌
```

---

### المشكلة 3: "الرسائل مكررة (جروب + فردي)"

هذا يحدث إذا:
- `send_to_group = 1` و الجروب موجود
- البوت يرسل أيضاً للأفراد

**الحل:**
```bash
# تأكد أن send_to_group = 1
sqlite3 data/family_assistant.db
UPDATE families SET send_to_group = 1 WHERE id = 1;
```

---

## 📝 ملاحظات مهمة

### ⚠️ تحذيرات:

1. **لا تحذف الجروب** بعد إعداده، وإلا ستحتاج إعداد جديد
2. **لا تزيل البوت** من الجروب
3. **احفظ معرف الجروب** في مكان آمن

### 💡 نصائح:

1. سمّي الجروب بوضوح (مثلاً: "عائلة أحمد - المساعد")
2. اجعل الجروب **خاصاً** فقط للأب والأم والبوت
3. فعّل **الإشعارات** للجروب لعدم تفويت الرسائل
4. يمكنك **تثبيت** الجروب في أعلى المحادثات

---

## 🎉 تهانينا!

إذا وصلت لهنا ونجحت الاختبارات، فأنت الآن جاهز لاستقبال رسائل المساعد الذكي في الجروب العائلي!

### الخطوات التالية:

1. ✅ شغّل البرنامج: `npm start`
2. ✅ انتظر الرسائل اليومية
3. ✅ تفاعل مع الأزرار
4. ✅ استمتع بالتجربة!

---

## 🆘 بحاجة لمساعدة؟

إذا واجهت مشاكل:

1. راجع قسم **استكشاف الأخطاء** أعلاه
2. شغّل `npm run test:all` للفحص الشامل
3. تحقق من السجلات في `logs/app.log`
4. افتح Issue على GitHub

---

**صُنع بـ 💙 للعائلات العربية**

استمتعوا بتجربة موحدة ومنظمة! 🎊
